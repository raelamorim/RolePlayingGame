version: '3.8'
services:
  api:
    build:
      context: .
      dockerfile: src/RolePlayingGame.Api/Dockerfile
    image: roleplayinggame-api:latest
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:80
    ports:
      - "5000:80"
    restart: always
